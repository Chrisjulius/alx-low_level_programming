#include "main.h"
/**
  * add  - add two integers and return the value.
  * @a: first operand for addition
  * @b: second operand for addition
  * Return: return value
  */
int add(int a, int b)
{
	int value;

	value = a + b;
	return (value);
}
